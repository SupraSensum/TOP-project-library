const myLibrary = [];

function Book(author, title, numPages, read = false, yearPublished) {
   this.author = author;
   this.title = title;
   this.numPages = numPages;
   this.read = read;
   this.yearPublished = yearPublished;
}

function addBookToLibrary() {
   
}